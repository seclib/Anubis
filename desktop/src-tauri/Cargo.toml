[package]
name = "anubis-desktop"
version = "0.1.0"
description = "Anubis Desktop OS"
edition = "2021"

[build-dependencies]
tauri-build = { version = "=2.0.0", features = [] }

[dependencies]
tauri = { version = "=2.0.0", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
