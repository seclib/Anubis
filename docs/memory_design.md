# Memory Design

ANUBIS separates:

- episodic memory: task and execution episodes;
- semantic memory: durable knowledge records;
- swarm memory: per-session shared research contributions;
- vector memory: deterministic local vector abstraction.

Memory writes are policy checked and conflict resolution is explicit.
