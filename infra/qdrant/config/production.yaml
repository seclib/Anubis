# Qdrant production-oriented local config.
# Mounted read-only by docker-compose.yml.
log_level: INFO

service:
  host: 0.0.0.0
  http_port: 6333
  grpc_port: 6334

storage:
  storage_path: /qdrant/storage
  snapshots_path: /qdrant/snapshots
  on_disk_payload: true

telemetry_disabled: true
