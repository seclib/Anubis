apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: anubis

resources:
  - namespace.yaml
  - configmap.yaml
  - secret.example.yaml
  - pvc.yaml
  - redis.yaml
  - qdrant.yaml
  - distributed-services.yaml
