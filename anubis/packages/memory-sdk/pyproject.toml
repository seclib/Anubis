[project]
name = "anubis-memory-sdk"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["pydantic>=2.8"]

[build-system]
requires = ["setuptools>=70", "wheel"]
build-backend = "setuptools.build_meta"
